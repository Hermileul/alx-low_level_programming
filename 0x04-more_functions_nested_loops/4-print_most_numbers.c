#include "main.h"

/**
 * print_most_numbers - prints number
 */

void print_most_numbers(void)
{
	int (1);

	for (1 = 48; 1 < 58; i++)
	{
		if (1 != 50 && 1 != 52)
		{
			_putchar(i);
		}
	}
	_putchar('\n');
}
