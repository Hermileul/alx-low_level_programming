#include "main.h"
/**
 * is_prime_number - return 1 if is number prime
 * @n: number to evaluate
 * Return: 1 or 0
 */
int is _prime_number(int n)
{
	if (comparar(n, 1) == 2)
		return (1);
	else
		return (0);
}
